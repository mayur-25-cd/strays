# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Build output
dist/*.app/
dist/*.dmg
dist/*.zip
dist/AppIcon.iconset/

# macOS
.DS_Store

# Xcode (if opened there)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/

# Local signing / notarization secrets — never commit
*.p12
*.cer
notary-*.json
.env

# Private maintainer notes (keep local, never publish)
*.local.md
